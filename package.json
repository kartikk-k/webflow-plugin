{
  "name": "ext",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "homepage": "https://kartikk-k.github.io/webflow-plugin/",
  "scripts": {
    "build": "npm install && tsc -p tsconfig.json && webflow extension bundle",
    "dev": "npm install && concurrently -r \"webflow extension serve\" \"tsc -p tsconfig.json --watch --preserveWatchOutput\""
  },
  "devDependencies": {
    "@types/bluebird": "^3.5.42",
    "@webflow/designer-extension-typings": "^0.1.11",
    "concurrently": "*",
    "typescript": "*"
  },
  "dependencies": {
    "webflow-cli": "1.6.4"
  }
}